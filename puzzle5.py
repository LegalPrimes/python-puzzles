#Puzzle #5
import random
yy = []
for xx in range (20):
        yy.append(random.randint (1,100))
yy_sorted = sorted(set(yy))
print ("the number of elements (a.k.a. length) in yy is %$for xx in range (-1,len(yy_sorted)):
        print (yy_sorted[xx])
